apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: catalogo-pod-cpu-stress
  namespace: app
  labels:
    app.kubernetes.io/name: catalogo
    chaos-test: "true"
spec:
  mode: one
  selector:
    namespaces:
      - app
    labelSelectors:
      app: catalogo
  stressors:
    cpu:
      workers: 1
      load: 80
  duration: "60s"
  scheduler:
    cron: "@every 10m"
