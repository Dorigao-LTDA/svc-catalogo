apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: catalogo-pod-kill
  namespace: app
  labels:
    app.kubernetes.io/name: catalogo
    chaos-test: "true"
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - app
    labelSelectors:
      app: catalogo
  duration: "60s"
  scheduler:
    cron: "@every 5m"
